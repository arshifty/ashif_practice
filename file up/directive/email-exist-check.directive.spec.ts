import { EmailExistCheckDirective } from './email-exist-check.directive';

describe('EmailExistCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailExistCheckDirective();
    expect(directive).toBeTruthy();
  });
});
