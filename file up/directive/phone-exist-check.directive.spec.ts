import { PhoneExistCheckDirective } from './phone-exist-check.directive';

describe('PhoneExistCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneExistCheckDirective();
    expect(directive).toBeTruthy();
  });
});
